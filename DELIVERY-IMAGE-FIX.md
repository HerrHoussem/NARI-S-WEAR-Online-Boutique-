# Delivery image fix

The repository now references:

`./assets/images/delivery-banner.png?v=5`

The delivery section uses a real `<img>` element, not placeholder text or only a CSS background.

Verified file:

`assets/images/delivery-banner.png`

After uploading to GitHub Pages, refresh with Ctrl+F5.
