# Mobile language and theme fix

The controls were previously moved inside the hamburger menu to prevent header overlap.

This update adds an always-visible mobile preference bar directly below the header:
- Light/dark mode toggle
- French/Arabic language toggle
- Dynamic labels reflecting the current state
- No overlap with logo, wishlist, cart or menu icons

Desktop behavior and all delivery, map, checkout and admin features remain unchanged.
