# Final fixes

- Removed the visible “Mode démonstration actif…” message.
- Demo products still load silently when Supabase is not configured.
- Re-cropped the hero artwork to remove the cut-off “Nari's Wear” text.
- Re-centered the model for desktop and mobile.
- Updated the hero image cache version to `v=3`.

After uploading to GitHub, wait for Pages deployment and refresh with Ctrl+F5.
